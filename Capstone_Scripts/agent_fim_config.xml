<agent_config>
  <syscheck>
    <!-- EXPLANATION: Set to realtime so that the moment a file becomes .encrypted, it alerts immediately -->
    <directories realtime="yes">/tmp/important_files</directories>
  </syscheck>
</agent_config>